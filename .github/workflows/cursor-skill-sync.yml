name: cursor-skill in sync

# Fails if .cursor/skills/meini-style/SKILL.md drifts from the canonical
# skills/meini-style/SKILL.md. The .cursor copy is a real file, not a
# symlink, so Windows clones and GitHub ZIP downloads work (#55).
on:
  pull_request:
    paths:
      - skills/meini-style/SKILL.md
      - .cursor/skills/**
      - .github/workflows/cursor-skill-sync.yml
  push:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Fail if the .cursor copy differs from SKILL.md
        run: |
          cmp skills/meini-style/SKILL.md .cursor/skills/meini-style/SKILL.md || {
            echo "::error::.cursor copy is out of sync. Run: cp skills/meini-style/SKILL.md .cursor/skills/meini-style/SKILL.md"
            exit 1
          }
